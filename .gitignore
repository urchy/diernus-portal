# Generated by seed-staging.sh — committed only as .example
tests/.staging-fixtures.json

# Wrangler's first deploy installs workerd + sharp into node_modules;
# these were never in the repo before. Ignore the whole tree.
worker/node_modules/
# macOS noise
.DS_Store

# Wrangler build/cache (auto-generated)
worker/.wrangler/
worker/dist/
